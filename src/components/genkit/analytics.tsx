'use client';

// The Genkit analytics component (`GenkitClientTracer`) seems to have been changed or removed in a recent update.
// This component is temporarily disabled to allow the application to build.
// You can track updates on Genkit's official documentation for the new client-side tracing API.

export function Analytics() {
  return null;
}
